# apps

Place Argo CD `Application` manifests here.

Each manifest in this directory should usually point to an application-specific
path in a deployment repository or to another path in this repository.
